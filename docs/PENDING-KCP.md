# 보류 작업 — KCP 휴대폰 본인인증(V2) 연동

> 상태: **보류 중**. 모든 다른 작업 완료 + 도메인 구입 후 진행한다.
> (도메인 미구입 상태 / 2026-05-25 기준)

## 결정된 사항
- 인증 수단: **NHN KCP 휴대폰본인확인 서비스 (신규 연동방식 V2)**
- V2 = "암호화된 인증 결과를 **Server-to-Server** 방식으로 요청·전달"
  → 바이너리 복호화 모듈 불필요 → **Cloudflare Pages Functions에서 동작 가능성 높음**
  (단, V2 매뉴얼의 복호화 방식 최종 확인 필요)

## 확보된 값 (비밀키 아님 — 식별자)
- 사이트코드(site_cd): `SM1WO` (상점명: 마타운)
- 웹사이트코드: `J25041411300`
- 인증결과URL 현재 등록 도메인: `matown.kr`, `www.matown.kr`

## 진행 전 블로커 / 필요한 것
1. **도메인** — 사이트 도메인은 `hobbaclub.vip`로 확정·연결됨. KCP "인증결과URL 설정"에
   `hobbaclub.vip`(및 `www.hobbaclub.vip`)를 추가 등록해야 함. (현재 KCP엔 matown.kr만 등록됨)
2. **V2 연동 매뉴얼** (개발지원센터 → 휴대폰본인확인 V2 / S2S 가이드)
   - 특히: 인증창 요청 파라미터 / 결과 복호화(키·암호화 방식)
3. **사이트키(시크릿)** — 받으면 코드에 직접 넣지 말고 **환경변수**로.

## 구현 시 작업 목록 (착수하면)
- [ ] 인증창 호출 프론트(`window.__kcpCert`) — 이미 19+ 게이트에 hook 남겨둠 (`scripts/templates.py` JS)
- [ ] 콜백/결과조회 엔드포인트 (`functions/api/kcp-cert.js` 신규)
- [ ] 인증 성공 시 성인확인 저장 → 게이트 통과 처리
- [ ] KCP "인증결과URL 설정"에 실제 콜백 URL 등록
- [ ] 개발 테스트 완료 후 **마지막에** V2 사용여부 '미사용→사용' 전환
      (★한번 '사용'하면 '미사용'으로 못 되돌림)

## 이미 되어 있는 것
- 19+ 성인 인증 게이트(전 페이지) + `window.__kcpCert` 연동 hook — 연동 전까지 자가확인 폴백
